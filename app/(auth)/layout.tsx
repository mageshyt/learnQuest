import "@/app/styles/pattern.css";

const AuthLayout = ({ children }: { children: React.ReactNode }) => {
  return <div className="  mt-4 overflow-hidden    text-left">{children}</div>;
};

export default AuthLayout;
